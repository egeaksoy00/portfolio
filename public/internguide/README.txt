InternGuide showcase files

Deploy these files under /internguide:
- index.html
- style.css
- script.js
- all existing PNG screenshot assets

Important privacy review before publishing:
- Replace or redact any real connected Gmail address.
- Redact real recipient email addresses and personal names unless they are deliberately public demo data.
- Prefer demo company/domain data where possible.
- Never include .env files, API keys, OAuth secrets/tokens, SMTP passwords, or local filesystem paths.

The HTML references screenshot filenames already included in the original project folder.
